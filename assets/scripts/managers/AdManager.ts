// AdManager.ts
export class AdManager {
    // 激励视频（复活用）
    static showRewardedVideo(callback: () => void) {
        // 调用原生广告SDK
        // 成功播放后执行callback
    }
    
    // 插屏广告（游戏结束展示）
    static showInterstitial() {
        // 展示插屏广告
    }
    
    // Banner广告（首页底部）
    static showBanner() {
        // 展示Banner
    }
}